config:
  target: "http://localhost:8545"
  phases:
    - duration: 60
      arrivalRate: 100
  processor: "./examples/13-paymaster.ts"  # Your TypeScript file

scenarios:
  - flow:
      - function: "transfer"